> 本文由Jzwalliser原创，发布在CSDN平台上，遵循[CC 4.0 BY-SA](https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans)协议。
> 因此，若需转载/引用本文，请注明作者并附原文链接。
> 违者必究，谢谢配合。
> 个人主页：[blog.csdn.net/jzwalliser](https://blog.csdn.net/jzwalliser)
# 题目
洛谷 [P1035 [NOIP2002 普及组] 级数求和](https://www.luogu.com.cn/problem/P1035)

> ## [NOIP2002 普及组] 级数求和
>
> ### 题目描述
>
> 已知：$S_n= 1+\dfrac{1}{2}+\dfrac{1}{3}+…+\dfrac{1}{n}$。显然对于任意一个整数 $k$，当 $n$ 足够大的时候，$S_n>k$。
>
> 现给出一个整数 $k$，要求计算出一个最小的 $n$，使得 $S_n>k$。
>
> ### 输入格式
>
> 一个正整数 $k$。
>
> ### 输出格式
>
> 一个正整数 $n$。
>
> ### 样例 #1
>
> #### 样例输入 #1
>
> ```
> 1
> ```
>
> #### 样例输出 #1
>
> ```
> 2
> ```
>
> ### 提示
>
> **【数据范围】**
>
> 对于 $100\%$ 的数据，$1\le k \le 15$。
>
> **【题目来源】**
>
> NOIP 2002 普及组第一题

# 想法
题目其实挺简单的，按照题目的意思，把公式套进去，然后循环，直到结果大于$k$就可以了。但是需要注意精度问题。当时用C++做这道题的时候，做了许多便都没有过，期间尝试了很多精度优化方式都失败了，直到后来才注意到我用的是数据类型`float`，精度不够；改成`double`后就通过了。

# 实现
1. 输入。
2. 循环，向累加器中增加$\frac{1}{i}$。
3. 判断是否大于$k$，如果满足条件则跳出循环。
4. 输出。

# 题解
## C++
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int k;
	int n = 0;
	cin >> k; //输入
	double s = 0; //累加器，注意double类型
	while(s <= k){
		n += 1; //当前数加一
		s += double(1) / n; //注意强制类型转换一下
	}
	cout << n; //输出
}
```

# Python
```py
k = int(input()) #输入
n = 0
s = 0 #累加器
while s <= k:
    n += 1 #当前数加一
    s += 1 / n
print(n) #输出
```

# 难度
难度：★☆☆☆☆
这道题难度不高，但是注意数据类型为`double`。不知当年比赛时选择了`float`作为数据类型的选手们比赛结束拿到结果有多奔溃……

# 结尾
这道题你是怎样写的？欢迎讨论！

